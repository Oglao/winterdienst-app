# PostgREST Configuration
# Database connection
db-uri = "postgres://postgres:password@localhost:5432/winterdienst"
db-schema = "public"
db-anon-role = "web_anon"
db-pool = 10

# API settings
server-host = "127.0.0.1"
server-port = 3001

# JWT settings
jwt-secret = "winterdienst_super_secure_jwt_secret_2024"
jwt-aud = "winterdienst-api"

# Logging
log-level = "info"

# CORS
cors-origin = "http://localhost:3000"

# OpenAPI
openapi-server-proxy-uri = "http://localhost:3001"